-- To view the tables and their structure, use Django’s database shell. Open the DB shell by running:
    python manage.py dbshell

-- Then run the SQLite commands:
    a.) .tables: 
    b.) .schema T2_album:
    c.) .schema T2_musician


-- Data Validation:
1.) python manage.py shell
2.) from T2.models import RajModel
3.) obj = RajModel(raj_field = "I am Raj")
4.) obj.full_clean()
NOTE: This will show an error of ValidationError because raj_field in an IntegerField but we are giving CharField
5.) from T2.models import RajModel
6.) obj = RajModel(raj_field = 7)
7.) obj.full_clean()
NOTE: This will not show any error
8.) 0bj.save(): It will save the value
9.) exit()



-- Custom Validation
1.) python manage.py shell
2.) from T2.models import customField
3.) from django.core.exceptions import ValidationError
4.) g = customField(mail="dbfbfhngmb@nn.com")
5.) g.full_clean()
NOTE: This will show ValidationError



Django ORM - Inserting, Updating & Deleting Data
1.) python manage.py shell
2.) from T2.models import Album2, Song2
a.) Insert
>>> a = Album2(title="Divide", artist="Ed Sheeran", genre="Pop")
>>> a.save()

b.) Retrieving
>>> Album.objects.all()
>>> Album.objects.filter(artist="The Beatles")
>>> Album.objects.exclude(genre="Rock")

c.) Updating
>>> Album.objects.filter(artist="The Beatles").update(genre="Classic Rock")

d.) Deleting
>>> Album.objects.filter(genre="Pop").delete()