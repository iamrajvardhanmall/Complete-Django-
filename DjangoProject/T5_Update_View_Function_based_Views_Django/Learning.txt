--> An Update View is used to edit or update an existing record in the database, such as a post, product, or user profile.
a.) Retrieves the record from the database using its ID or another unique value.
b.) Shows a form already filled with the current data.
c.) Checks the submitted form for correctness.
d.) Saves the updated record to the database if the data is valid.

CODE: In app_name/models.py:

--> After creating this model, we need to run two commands in order to create Database for the same. 
CODE:
    Python manage.py makemigrations
    Python manage.py migrate

CODE: In app_name/admin.py(register model in admin.py)

--> Now we will create a Django ModelForm for this model. Create a file forms.py in geeks folder:
CODE: In app_name/forms.py

--> For Update_view one would need some identification to get a particular instance of the model. Usually it is unique primary key such as id. To specify this identification we need to define it in urls.py.
CODE: In app_name/urls.py

--> Let's create these views with explanations. 
CODE: In app_name/views.py:

--> Now create following templates in templates folder. 
CODE: In app_name/templates/update_view.html:
CODE: In app_name/templates/detail_view.html:

--> Let's check if everything is working, visit http://localhost:8000/app_name/1/update:

